{
  "name": "web",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/web/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "serve-with-backend": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx dev convex", "nx serve nx-convex"],
        "parallel": true
      }
    }
  }
}
